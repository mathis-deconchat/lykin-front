query getAllSpendings {
  spendings {
    nodes {
      createdAt
      id
      categoryId
    }
  }
}

mutation createSpending($input: CreateSpendingInput!) {
  createSpending(input: $input){clientMutationId}
}
